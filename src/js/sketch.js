export default function sketch (p) {
    let rotation = 0;
  
    p.setup = function () {
      p.createCanvas(600, 400);
    };
  

  
    p.draw = function () {
        p. background(200,100,200);
    };
  };